apiVersion: apps/v1
kind: Deployment
metadata:
  name: select-ai-worker
  namespace: select-ai-gateway
spec:
  replicas: WORKER_REPLICAS
  selector:
    matchLabels:
      app: select-ai-worker
  template:
    metadata:
      labels:
        app: select-ai-worker
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: worker
          image: WORKER_IMAGE
          command:
            - select-ai
          args:
            - a2a
            - worker
            - --host
            - 0.0.0.0
            - --port
            - "8080"
            - --consul-url
            - http://consul-server.select-ai-gateway.svc.cluster.local:8500
            - --worker-id
            - $(WORKER_ID)
            - --session-ttl-seconds
            - "SESSION_TTL_SECONDS"
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: WORKER_ID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: WORKER_ADDRESS
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
          readinessProbe:
            httpGet:
              path: /health
              port: http
          livenessProbe:
            httpGet:
              path: /health
              port: http
